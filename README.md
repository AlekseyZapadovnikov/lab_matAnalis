# Root Finder via Bisection Method

Этот скрипт находит корень функции  
\[
f(x) = \arcsin\left(\frac{2x}{1 + x^2}\right) - e^{-x^2}
\]  
на заданном отрезке с помощью **метода бисекции** (дихотомии).

## 📌 Использование

Скрипт принимает два аргумента командной строки — концы отрезка `[a, b]`, на котором ищется корень:

```bash
python main.py a b
